# No environment variables are required for this SPA.
# All image URLs are hardcoded in src/lib/assets.ts and point to Cloudinary.
# To use a custom CDN, replace the URLs in src/lib/assets.ts before building.
